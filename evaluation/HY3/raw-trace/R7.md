# R7 — 原始留痕登记

- **round**：R7（安全 / 性能加压）
- **发出的 Prompt**：`round-evidence/R7/prompt.md`（与 `instruction.md` §7「R7」逐字一致）
- **派发模型**：`hy3`｜同一连续工作区 `trial-workspaces/hy3`
- **模型输出/留痕**：`../final-artifact/TRACE_R7.md`（模型亲笔）
- **代码变化 / 最终状态证据**（评测方差分核验）：
  `MOD app.py`（订单/售后端点加 `X-User-Id` 归属校验；审核端点加 `X-User-Role` 门）；
  `MOD store.py`（**两阶段退款**：认领+预占 → 事务外调渠道 → 结算；加索引与 `limit/offset` 分页）；
  `MOD models.py`；**新增 `tests/test_security_r7.py`**；新增 `TRACE_R7.md`。
- **测试结果**：**55 passed**（43 → 55；评测方独立复跑。R8 的 `TRACE_R8.md` 亦记「R8 前为 55 passed」，两处互证）
- **trace id / request id**：`MISSING`
- **状态**：已登记

> **本轮的已知缺口（真实缺陷，非漏记）**：审核端点**只校验 `X-User-Role`，不校验 `X-User-Id` 归属**，
> 且无角色头时直接放行 → 普通用户可 `approve` 他人售后（自评自批）。该缺口在 R9 未被修复，
> 由统一 Golden `test_security.py::test_unauthorized_after_sale_access` 在双模式下判 FAIL，
> 落地为 Rubric **FD-08 FAIL**（`../result.md` §5）。模型 README 第 14 条却声称该保证已满足 → 文档虚标。
