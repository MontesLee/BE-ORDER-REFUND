# R6 — 原始留痕登记

- **round**：R6（线上事故：第三方退款失败却本地显示成功）
- **发出的 Prompt**：`round-evidence/R6/prompt.md`（与 `instruction.md` §7「R6」逐字一致）
- **派发模型**：`hy3`｜同一连续工作区 `trial-workspaces/hy3`
- **模型输出/留痕**：`../final-artifact/TRACE_R6.md`（模型亲笔）
- **代码变化 / 最终状态证据**（评测方差分核验）：
  **新增 `gateway.py`** —— 可替换 `RefundGateway` 抽象（可编程确定性成功/失败）；
  `MOD models.py`（新增 `REFUND_FAILED` 状态）、`MOD store.py`（失败分支置 `REFUND_FAILED`、释放额度、不缓存幂等键、重试成功置 `REFUNDED`）、`MOD README.md`；
  **新增 `tests/test_refund_gateway.py`**（12 个确定性网关用例）；
  新增 `TRACE_R6.md`。
- **测试结果**：**43 passed**（34 → 43；评测方独立复跑）
- **trace id / request id**：`MISSING`
- **状态**：已登记

> 根因判定正确：此前**本地从未调用第三方渠道**即标记 `REFUNDED`——该事故在本代码里是真实存在的，
> 模型定位并修复，正面命中不变量 I4。契约偏差：渠道失败返回 HTTP **502**（语义正确的 Bad Gateway），
> 与 canonical 断言要求的小于 500 不同 —— 记录为偏差，不改变 I4 成立的事实。
