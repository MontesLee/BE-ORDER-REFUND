# R3 — 原始留痕登记

- **round**：R3（技术取舍：Multi-worker 并发保护）
- **发出的 Prompt**：`round-evidence/R3/prompt.md`（与 `instruction.md` §7「R3」逐字一致）
- **派发模型**：`hy3`｜同一连续工作区 `trial-workspaces/hy3`
- **模型输出/留痕**：`../final-artifact/TRACE_R3.md`（模型亲笔）
- **代码变化 / 最终状态证据**（评测方差分核验）：
  **`MOD store.py`（架构级改造）**——由进程内内存字典改为**共享 SQLite**：
  `BEGIN IMMEDIATE` 写事务 + 单条带条件 `UPDATE` 预占额度 + `idempotency` 表；
  新增 `tests/test_concurrency.py`（**真实多进程**并发用例，断言业务结果）；
  新增 `TRACE_R3.md`。
- **测试结果**：**30 passed**（24 → 30；含多进程 `test_multiprocess_same_after_sale_only_one_refund`、
  `test_multiprocess_multiple_after_sales_over_budget`；评测方独立复跑）
- **trace id / request id**：`MISSING`
- **状态**：已登记

> 本轮是全程最关键的架构转折：模型自己发现并修掉了 R2 遗留的「进程内存幂等」局限，
> 改用跨进程有效的数据库机制 —— 正面规避了失效模式 #7（进程内锁冒充分布式保护）。
