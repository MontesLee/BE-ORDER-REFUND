# R8 — 原始留痕登记

- **round**：R8（测试要求：补齐六类自动化测试并保证稳定可重复）
- **发出的 Prompt**：`round-evidence/R8/prompt.md`（与 `instruction.md` §7「R8」逐字一致）
- **派发模型**：`hy3`｜同一连续工作区 `trial-workspaces/hy3`
- **模型输出/留痕**：`../final-artifact/TRACE_R8.md`（**评测方据产物重建**，见该文件首注）
  派发时 Agent 工具返回 HTTP 429，模型的最终回复（含 `POWERED-BY`）被截断，未回传。
  **这不是「未执行」**：工作区已落地 `tests/test_round8.py` 并独立复跑通过（见下）。
- **代码变化 / 最终状态证据**（评测方差分核验）：
  **新增 `tests/test_round8.py`**（22 个确定性用例，覆盖 Business / State / Idempotency / Concurrency /
  Third-party / Security 六类共 14 子项）；核心代码无回归。另有子 Agent 遗留的临时 `_*.txt` 与
  `.pytest_cache`（非交付物，已隔离，未进入冻结产物）。
- **测试结果**：**76 passed**（55 → 76；评测方独立复跑确认，先于模型自报采信）
- **trace id / request id**：`MISSING`（该次派发触发的 429 回执 id 见 `README.md` §4，非本轮的 trace id）
- **状态**：已登记（429 ≠ 未执行，按工作区证据判定真实完成）

> 方法论要点：平台返回 429 时**必须先核验工作区**再决定是否重试，否则同一轮会被重复施加、污染 Trial。
